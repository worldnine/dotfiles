{
  // nvim 全体で使う markdownlint のグローバル設定（nvim-lint が --config で参照）。
  // 日本語の散文に「1行80文字まで」(MD013) は実質ナンセンスなため無効化する。
  "MD013": false,
  // MD060 (table-column-style): テーブルのパイプ揃えの一貫性チェック。
  // 全角(日本語)混じりのテーブルは生ソースでは桁が揃わず必ず違反になるが、
  // 表示は render-markdown が全角幅で揃え直すため実害なし。よって無効化する。
  "MD060": false
}
